SCENARIO_1.ipynb

EDA on an e-commerce transactions CSV (541,909 rows).
Shows head/tail, null counts, summary stats, top products by quantity and simple bar/line plots.
Note: many missing CustomerID and some negative quantities/prices (returns).

SCENARIO_2.ipynb

EDA on Pima Indians Diabetes dataset (768 rows).
Summary stats, histogram of Glucose, boxplot of Age.
Note: zeros in clinical fields likely represent missing values that need imputation.

SCENARIO_3.ipynb

EDA on a housing dataset (~545 rows).
Scatter plot area vs price, correlation matrix heatmap, no nulls reported.
Next: encode categoricals and try regression to predict price.

SCENARIO_4.ipynb

EDA on a marketing/customer dataset (2,240 rows, 29 cols).
Shows head/info/describe and null-counts (Income has ~24 missing).
Next: parse Dt_Customer, impute Income, encode categories for modeling.# Machine-Learning
lab experiment-1
